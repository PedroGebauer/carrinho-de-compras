{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug no navegador",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:8000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "serve"
    }
  ]
}
